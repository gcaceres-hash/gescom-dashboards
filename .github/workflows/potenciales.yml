name: Clientes Potenciales

on:
  schedule:
    - cron: "45 10 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  actualizar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Escribir config.json desde el secret
        shell: bash
        run: echo '${{ secrets.GESCOM_CONFIG }}' > scripts/config.json

      - name: Correr pull-potenciales.ps1
        shell: pwsh
        run: ./scripts/pull-potenciales.ps1

      - name: Publicar cambios (commit + push a docs/potenciales)
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/potenciales
          git diff --cached --quiet || git commit -m "Actualiza Clientes Potenciales"
          git push
